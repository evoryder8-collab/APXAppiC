# Supabase project credentials (used from phase 2 onward).
# The anon key is safe to ship in the client bundle because every table
# is protected by Row Level Security scoped to the authenticated user.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
